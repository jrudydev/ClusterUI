import Foundation
import CoreFoundation

struct Constants {
  
  enum AnchorMagnitude {
    static let ring = CGPoint(x: -0.0285, y: -0.085)
    static let ray = CGPoint(x: 0.05, y: 0.048)
    static let swirl = CGPoint(x: 0.05, y: 0.05)
  }
  
  static let transMinScale = 0.9
  static let transMaxScale = 1.05
  
}
